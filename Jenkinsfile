pipeline {
    agent any

    stages {
        stage('Checkout Test') {
            steps {
                echo 'Jenkinsfile loaded successfully from GitHub'
            }
        }

        stage('Build') {
            steps {
                echo 'Hello from Jenkins pipeline'
            }
        }
    }
}
